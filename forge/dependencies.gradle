dependencies {
    api libs.kumaForge
    jarJar(group: "net.blay09.mods", name: "kuma-api-forge", version: kuma_version_range) {
        jarJar.pin(it, kuma_version)
        transitive = false
    }
    compileOnly jeiCommon
    compileOnly curiosForge
    compileOnly wthitForge
}